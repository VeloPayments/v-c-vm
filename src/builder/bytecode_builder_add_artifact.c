#include <vpr/parameters.h>
#include <vcvm/builder.h>
#include <vpr/allocator.h>

int bytecode_builder_add_artifact(bytecode_builder_t* UNUSED(builder), uint8_t* UNUSED(artifact_uuid))
{
    return 0;
}