#include <vpr/parameters.h>
#include <vcvm/builder.h>
#include <vpr/allocator.h>

int bytecode_builder_emit(bytecode_builder_t* UNUSED(builder), uint8_t* UNUSED(bytes), size_t UNUSED(size))
{
    return 0;
}